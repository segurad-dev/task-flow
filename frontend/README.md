# Task Flow — Frontend Demo

Минималистичный SPA-интерфейс для демонстрации работы Task Flow API.

**Стек**: HTML + [Alpine.js](https://alpinejs.dev) (CDN) + [Tailwind CSS](https://tailwindcss.com) (CDN)  
Сборка не требуется — всё загружается через CDN.

## Быстрый старт

### 1. Запустите бэкенд

```bash
# В корне проекта
docker-compose up
```

API должен быть доступен на `http://localhost:8000`.

### 2. Запустите фронтенд

```bash
# Из папки frontend/
cd frontend
python -m http.server 3000
```

Откройте **http://localhost:3000** в браузере.

> **Почему нельзя просто открыть index.html двойным кликом?**  
> Браузер блокирует загрузку модулей (`api.js`, `app.js`) через протокол `file://`.  
> Локальный HTTP-сервер обходит это ограничение.

## Что умеет интерфейс

| Функция | Описание |
|---|---|
| Регистрация / Вход | JWT-токен сохраняется в `localStorage` |
| Проекты | Список, создание, переключение |
| Задачи | Список с фильтром по статусу, создание, смена статуса, удаление |
| Аналитика | Прогресс-бар, счётчики по статусам, среднее время выполнения |
| Обработка ошибок | Toast-уведомление при ошибках API, авто-выход при истёкшей сессии |

## Структура файлов

```
frontend/
├── index.html   # SPA-оболочка: разметка, Alpine-компоненты, CDN-скрипты
├── config.js    # API_BASE_URL — меняйте при деплое бэкенда
├── api.js       # Все fetch-запросы к бэкенду, обработка ошибок
└── app.js       # Alpine-компоненты: authApp, dashboardApp
```

## Конфигурация

По умолчанию фронт обращается к `http://localhost:8000`.  
Чтобы указать другой адрес бэкенда, отредактируйте [config.js](config.js):

```js
const API_BASE_URL = "https://your-backend.railway.app";
```

## Деплой на GitHub Pages

GitHub Pages поддерживает только статические файлы — бэкенд нужно деплоить отдельно
(например, на [Railway](https://railway.app) или [Render](https://render.com)).

После деплоя бэкенда:
1. Обновите `API_BASE_URL` в `config.js`
2. Добавьте домен GitHub Pages в `allow_origins` в `app/main.py`
3. Скопируйте содержимое `frontend/` в корень ветки `gh-pages`
